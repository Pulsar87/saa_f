import '../constant/app-const.dart';
import '../network/local/shared_prefrences_helper.dart';

Future getAppInfo() async {
  // AppConst.token =
  //     await SharedPreferencesHelper.getData(key: 'accessToken') ?? "";
  // AppConst.token = await SharedPreferencesHelper.getData(key: 'email') ?? "";
}
